server:
  port: 8085

spring:
  application:
    name: reservation

  eureka:
    client:
      service-url:
        default-zone: http://localhost:8761/eureka

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher