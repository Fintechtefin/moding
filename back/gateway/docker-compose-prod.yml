services:
  spring-boot:
    restart: 'on-failure'
    build:
      dockerfile: Dockerfile
      args:
        PROFILE: prod
    container_name: 'gateway-prod'
    ports:
      - 8081:8081
    networks:
      - mooding_network

networks:
  mooding_network:
