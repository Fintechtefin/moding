services:
  spring-boot:
    restart: 'on-failure'
    build:
      dockerfile: Dockerfile
      args:
        PROFILE: dev
    container_name: 'discovery-dev'
    ports:
      - 8761:8761
    networks:
      - mooding_network

networks:
  mooding_network:
